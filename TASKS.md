## 你的任务

完成下面第一个未完成的任务。一定记住你只应该完成一个任务，不要做少也不要做多！

请立刻用 TodoWrite 记录下面每一项，一项都不要漏，在记录之前不准开始任何行动！如果没有 TodoWrite 工具，你应该直接向用户提问，而不是进行任何行动！

- 完成任务前，创建一个新分支。
- 根据任务内容完成任务。
- 完整检查代码库、测试、文档，确保没有过时内容，不要让下一个工程师困惑。
- 视情况撰写单元测试，并确保所有测试成功。
- 确保 LSP 无错误、警告。
- 更新 TASKS.md 文档，标注任务已完成，并且简要概括完成情况，100 字以内。
- 完成任务后，把自己的工作合并至 main 分支。
- 如果还有剩余的任务，输出 {"all_done": false}，否则输出 {"all_done": true}

剩余任务将由其他工程师完成。

## 任务列表

### 3

- 状态：已完成 (2026-03-31)

checkout 不再返回目录而是记录在内部，新增 get_checkout_dir() 函数获取目录。如未调用过 checkout，则创建唯一临时目录。

### 4

- 状态：未完成

checkout 额外接受 path 参数，必须是相对路径，相对于调用 python 时的 pwd，表示在这里运行 git archive。

### 5 

- 状态：未完成

不在 git 仓库中要报错并且让 Agent 引导用户在合适位置创建 git 仓库。

### 6

- 状态：未完成

checkout 时在 /tmp/... 创建目录时应该加上 path 变为绝对路径转义后的结果作为文件目录名前缀。
