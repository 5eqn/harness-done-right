## 任务二完成说明
本次成功完成LLM提示词修复任务：
1. **核心修改**：移除了system prompt中要求输出<think>标签的规则，新增"基于第一性原理从正反两侧思考"的要求，保证评估公允性；要求分数使用<score>标签包裹（如<score>5</score>），提升解析可靠性。
2. **逻辑更新**：同步修改结果解析代码，不再依赖<think>标签提取思考过程，改为提取<score>标签前的所有内容作为思考记录，正确解析<score>标签内的分数值。
3. **兼容性**：所有单元测试全部通过，Mock模式运行正常，未破坏现有接口和功能。

后续任务三注意事项：需要为_llm_assert_call函数添加错误重试机制，遇到网络异常、权限错误等问题时最多重试10次，每次重试打印错误信息，重试失败再抛出异常，同时要避免错误结果被locache缓存。修改时请保持向后兼容性，完成后务必运行所有测试验证。